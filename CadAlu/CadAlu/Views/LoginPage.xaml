<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CadAlu.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center" >
            <Label x:Name="txtEmail" Text="email" FontSize="Medium" IsVisible="false"  Command="{Binding SetVisible}"/>
            <Entry x:Name="entName" Text="{Binding Email, Mode=TwoWay}" FontSize="Medium"  IsVisible="false"/>
            <Label x:Name= "txtPassword" Text="password" FontSize="Medium"  IsVisible="false"/>
            <Entry x:Name= "entPassword" Text="{Binding Password, Mode=TwoWay}" FontSize="Medium"  IsVisible="false"/>
            <Button x:Name="btnLogin" VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}" IsVisible="false"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>